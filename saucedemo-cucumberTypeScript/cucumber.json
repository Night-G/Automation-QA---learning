{
  "default": {
    "require": [
      "src/test/steps/**/*.ts",
      "src/hooks/hooks.ts"
    ],
    "format": ["progress"],
    "requireModule": ["ts-node/register"],
    "paths": ["src/test/features/**/*.feature"]
  }
}